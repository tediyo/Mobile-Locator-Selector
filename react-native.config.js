module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // Only FontAwesome is used in the app (smaller APK than bundling every icon font).
  assets: ['./node_modules/react-native-vector-icons/Fonts/FontAwesome.ttf'],
};
