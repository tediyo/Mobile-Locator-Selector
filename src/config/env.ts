/** Production API (Render). */
export const API_BASE_URL = 'https://twt-pktm.onrender.com';

/**
 * Set true to hit a local backend instead of API_BASE_URL.
 * Physical device: set DEV_MACHINE_HOST to your PC LAN IP.
 * Android emulator: uses 10.0.2.2 automatically in api.ts.
 */
export const USE_LOCAL_API = false;

export const DEV_MACHINE_HOST = '10.190.12.176';
export const API_PORT = 3001;
